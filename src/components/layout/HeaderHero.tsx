import React from "react";

/**
 * HeaderHero
 * Placeholder for the Hero area (Figma 'hero' region). Currently
 * the project header does not have a separate hero block, so this
 * file is reserved for a future 1:1 extraction.
 */
export default function HeaderHero(_: any) {
  return null;
}
